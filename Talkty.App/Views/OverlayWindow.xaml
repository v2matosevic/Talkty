<Window x:Class="Talkty.App.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Talkty"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <!-- Ultra-compact pill overlay -->
    <Border Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.4" Color="#000000"/>
        </Border.Effect>

        <Border CornerRadius="18"
                BorderThickness="1"
                BorderBrush="#20FFFFFF"
                Padding="12,8">
            <Border.Background>
                <SolidColorBrush Color="#F0121214"/>
            </Border.Background>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Audio Wave Icon that reacts to audio level -->
                <Grid Width="20" Height="16" Margin="0,0,8,0">
                    <!-- Wave bars that react to AudioLevel -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Bar 1 -->
                        <Border x:Name="Bar1" Width="3" CornerRadius="1.5" Margin="0,0,2,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Height" Value="6"/>
                                    <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTranscribing}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusPurpleBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusText}" Value="Copied!">
                                            <Setter Property="Background" Value="{StaticResource StatusGreenBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandBusy}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusPurpleBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandDone}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusGreenBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandFailed}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <!-- Bar 2 (center, taller) -->
                        <Border x:Name="Bar2" Width="3" CornerRadius="1.5" Margin="0,0,2,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Height" Value="12"/>
                                    <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTranscribing}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusPurpleBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusText}" Value="Copied!">
                                            <Setter Property="Background" Value="{StaticResource StatusGreenBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandBusy}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusPurpleBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandDone}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusGreenBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandFailed}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <!-- Bar 3 -->
                        <Border x:Name="Bar3" Width="3" CornerRadius="1.5" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Height" Value="8"/>
                                    <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTranscribing}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusPurpleBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusText}" Value="Copied!">
                                            <Setter Property="Background" Value="{StaticResource StatusGreenBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandBusy}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusPurpleBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandDone}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusGreenBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommandFailed}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource StatusRedBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- Timer / Status. A command takes the pill over: see below. -->
                <TextBlock VerticalAlignment="Center" MinWidth="40" MaxWidth="220"
                           TextTrimming="CharacterEllipsis" ToolTip="{Binding StatusText}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding StatusText}"/>
                            <Setter Property="Foreground" Value="#E4E4E7"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsListening}" Value="True">
                                    <Setter Property="Text" Value="{Binding ElapsedTime}"/>
                                </DataTrigger>
                                <!-- Distinguish prompt refinement from speech transcription. -->
                                <DataTrigger Binding="{Binding StatusText}" Value="Prompting…">
                                    <Setter Property="Text" Value="Prompting…"/>
                                    <Setter Property="Foreground" Value="{StaticResource StatusPurpleBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="Copied!">
                                    <Setter Property="Text" Value="Copied"/>
                                    <Setter Property="Foreground" Value="{StaticResource StatusGreenBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsCommand}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- While he is still speaking: what has been heard so far. -->
                <TextBlock Text="{Binding PreviewText}"
                           ToolTip="{Binding PreviewText}"
                           MaxWidth="420"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           FontSize="12.5"
                           FontFamily="Segoe UI Variable Text, Segoe UI"
                           Foreground="#A1A1AA"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding HasPreview}" Value="True"/>
                                        <Condition Binding="{Binding IsCommand}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Command mode: what he asked, then what happened to it. -->
                <StackPanel VerticalAlignment="Center" MaxWidth="420" Margin="0,0,2,0">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCommand}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="{Binding CommandText}"
                               ToolTip="{Binding CommandText}"
                               Foreground="#E4E4E7"
                               FontSize="13"
                               FontWeight="Medium"
                               FontFamily="Segoe UI Variable Text, Segoe UI"
                               TextTrimming="CharacterEllipsis"/>

                    <TextBlock Text="{Binding CommandDetail}"
                               ToolTip="{Binding CommandDetail}"
                               FontSize="11.5"
                               Margin="0,2,0,0"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#A1A1AA"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCommandDone}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource StatusGreenBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsCommandFailed}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource StatusRedBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- "esc" cancel hint — revealed on hover, like the sparkle. Makes the (previously
                     invisible) ESC-to-cancel feature discoverable without cluttering the pill. -->
                <TextBlock Text="esc cancels"
                           FontSize="10"
                           Foreground="#71717A"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <!-- Nothing to cancel once the result is on the clipboard -->
                                <DataTrigger Binding="{Binding StatusText}" Value="Copied!">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsCommand}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Nothing configurable lives on the pill: it is for watching
                     what is happening, not for setting anything. -->
            </StackPanel>
        </Border>
    </Border>
</Window>
